docs/design/tokens.md 를 읽고,
**$ARGUMENTS** 에 해당하는 토큰을 찾아 아래 형식으로 알려줘:

1. **토큰명**: 정확한 토큰 이름
2. **HEX 값**: 색상값
3. **용도**: 언제 사용하는지
4. **Tailwind 적용 예시**: `bg-[#HEX]`, `text-[#HEX]` 형태로 표시
5. **연관 토큰**: 함께 자주 사용되는 토큰 목록

키워드가 불분명하면 유사한 토큰 후보를 모두 나열해줘.
